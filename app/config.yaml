event:
  profile:
    traits:
      name: profile@data.pii.firstname
      surname: profile@data.pii.lastname
      email: profile@data.contact.email.main
  context: off
session:
  matadata: off
profile:
  pii: off