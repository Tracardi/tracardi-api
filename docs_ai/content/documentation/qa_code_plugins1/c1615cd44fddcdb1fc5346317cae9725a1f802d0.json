{"file_name": "docs/qa/code/plugins1.md", "questions": ["Whole Content: How does the PasswordGeneratorAction class work and what does it do?", "Partial Content:", "1. How does the set_up method of the PasswordGeneratorAction class set up the PasswordGenerator object?", "2. What does the run method of the PasswordGeneratorAction class return?", "3. What is the purpose of the config attribute in the PasswordGeneratorAction class?"], "answer": "# Action runner class for the pluginclass PasswordGeneratorAction(ActionRunner):\n    config: Config\n    pgo: PasswordGenerator\n\n    async def set_up(self, init):\n        self.pgo = PasswordGenerator()\n        self.config = validate(init)\n        self.pgo.minlen = self.config.min_length\n        self.pgo.maxlen = self.config.max_length\n        self.pgo.minuchars = self.config.uppercase\n        self.pgo.minlchars = self.config.lowercase\n        self.pgo.minschars = self.config.special_characters\n\n    async def run(self, payload: dict, in_edge=None) -> Result:\n        password = self.pgo.generate()\n        return Result(port=\"password\", value={\"password\": password})", "hash": "c1615cd44fddcdb1fc5346317cae9725a1f802d0"}