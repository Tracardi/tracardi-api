{"file_name": "docs/configuration/tracardi_ssl.md", "questions": ["COPY ./tracardi-api /app", "# SSL", "COPY ssl/cert.pem /etc/ssl/certs/cert.pem", "COPY ssl/key.pem /etc/ssl/private/key.pem", "ENV PORT=80", "EXPOSE 80", "CMD [\"uvicorn\", \"main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"80\", \"--ssl-keyfile\", \"/etc/ssl/private/key.pem\", \"--ssl-certfile\", \"/etc/ssl/certs/cert.pem\"]", "```", "Question: How to build Tracardi API with SSL certificates embedded into docker?", "Optional questions: ", "- What is the command to clone the Tracardi API repository?", "- What is the path to the SSL certificate and key file in the Dockerfile.ssl-internal?"], "answer": "## Building Tracardi API with SSL certificates embedded into docker\nClone repository `tracardi/tracardi-api.git`.\n\n```\ngit clone https://github.com/tracardi/tracardi-api.git\n```\n\nNext go to tracardi folder and find file **Dockerfile.ssl-internal** and type path to your SSL certificate and key file. \n\n* Find and replace `ssl/key.pem` with a path to your key file\n* Find and replace `ssl/cert.pem` with a path to your certificate\n\nThis is how the **Dockerfile.ssl-internal** looks like\n\n```\nFROM tiangolo/uvicorn-gunicorn-fastapi\n\nRUN apt-get update\nRUN apt-get install -y git"}